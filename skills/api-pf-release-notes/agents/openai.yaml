interface:
  display_name: "API-PF Release Notes"
  short_description: "Create API-PF release notes from .scratch/RN inputs."
  default_prompt: "Use $api-pf-release-notes to create a new .scratch/RN release_input.md template, with TODO as the fallback name, or generate an API-PF release note from a filled input."
